{
  "name": "@calclone/prisma",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "db-up": "docker compose up -d || docker-compose up -d",
    "db-studio": "yarn prisma studio",
    "generate-schemas": "prisma generate && prisma format",
    "db-migrate": "yarn prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^3.15.2",
    "zod": "^3.23.8"
  },
  "main": "index.ts",
  "types": "index.d.ts",
  "devDependencies": {
    "zod-prisma": "^0.5.4"
  }
}
